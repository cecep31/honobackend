{
  "scripts": {
    "dev": "bun run --hot src/main.ts",
    "build": "bun build src/main.ts --outdir dist --minify",
    "build:compile": "bun build src/main.ts --outfile bin/honobackend --minify --compile",
    "start": "bun run src/main.ts",
    "start:prod": "bun run dist/main.js"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.1",
    "@prisma/client": "^5.13.0",
    "bcryptjs": "^2.4.3",
    "hono": "^4.2.0",
    "prisma": "^5.13.0",
    "uuid": "^9.0.1",
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/bun": "latest",
    "@types/uuid": "^9.0.8"
  }
}
