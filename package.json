{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --minify --target bun",
    "build:compile": "bun build src/index.ts --outfile bin/honobackend --minify --compile",
    "start:prod": "bun run src/index.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.1",
    "bcryptjs": "^2.4.3",
    "drizzle-orm": "^0.30.10",
    "hono": "^4.2.0",
    "postgres": "^3.4.4",
    "uuid": "^9.0.1",
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/bun": "latest",
    "@types/uuid": "^9.0.8",
    "drizzle-kit": "^0.21.2"
  }
}
