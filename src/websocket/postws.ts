import { Hono } from "hono";
import {bu} from 'hono/bun'


setupWebscoket(app: Hono, upgradewebsocket: BunWebSocketHandler<BunWebSocketData>){
    postws.get("/", async (c) => {
        upgradewe
    })
}